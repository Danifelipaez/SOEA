namespace SOEA.Domain.Enums
{
    /// <summary>
    /// Identifica cada una de las dos semanas del ciclo de alternancia.
    /// Semana A = semanas impares (TipoA presencial / TipoB virtual).
    /// Semana B = semanas pares (TipoB presencial / TipoA virtual).
    /// </summary>
    public enum SemanaAcademica
    {
        A,
        B
    }
}
